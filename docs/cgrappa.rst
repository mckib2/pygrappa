.. cgrappa:

pygrappa.cgrappa
================

.. automodule:: pygrappa.cgrappa
   :members:

`cgrappa` is Cython implementation of GRAPPA.  It is faster than its Python counterparts, but is known to have bugs.  It is probably due for a rewrite in the style of `mdgrappa`.
